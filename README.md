# **No longer under development**

Friend group has shifted games. Also not liking some of the recent changes. 

This should be valid. It should. I don't care if your language convention prefers `m_` prefix or whatever. The fact this used to work but no longer does is absurd to me. 
```c
int age = 10;
void DoSomething(int age) {
   this.age = age;
}
```

Documentation - https://trainwreckers.github.io/documentation
